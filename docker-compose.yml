version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["sh", "-c", "python src/manage.py runserver 0.0.0.0:8000"]
    volumes:
      - ./:/app
    ports:
      - "8001:8000"